///|
/// Test the transition system
fn test_transition_system() -> Unit {
  // Test basic lexer creation
  let lexer = Lexer::new(source="123 abc")

  // Test that the lexer starts in Base transition
  // Note: We can't access lexer.transition directly since Lexer is abstract
  // Instead, we'll test the tokenization functionality

}

///|
/// Run the test
test "transition system" {
  
}
